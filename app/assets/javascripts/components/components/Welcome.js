import React, { Component, PropTypes } from 'react';

class Welcome extends Component {
  render() {
    return (
      <div>Welcome!</div>
    );
  }
}

Welcome.propTypes = {
  
};

export default Welcome;
